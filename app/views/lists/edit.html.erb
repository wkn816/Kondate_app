<h1>投稿編集画面</h1>
<%= form_with model:@list, local:true do |f| %>
  <label for="inputbody">内容</label>
  <%= f.text_area :introduction, autofocus: true, id:"inputBody"%> <br>
  <%= f.submit "更新" %>
<% end %>
<%= link_to "削除", list_path(@list), method: :delete %>